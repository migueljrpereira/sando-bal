[container.image]
repository="migueljrpereira" # Docker hub name
name="sandobal-api-sandwich" # container name
tag="alpha.0.5"

[settings]
buildImage=false # you can make this false if you don't want to build the image and you just need the Dockerfile
